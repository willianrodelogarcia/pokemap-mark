const pokemonController = require('../controllers/pokemon.controller');
const evolutionsController = require('../controllers/evolutions.controller');

module.exports = {
  pokemonController,
  evolutionsController,
};
